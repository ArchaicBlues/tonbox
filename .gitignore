node_modules/
*.log

# Credentials - never track these
.settings.conf
client_secret_*.json

# Compiled binaries (architecture-specific, not for source distribution)
/gramocli
/songrec

# Local/personal machine state - not relevant to other installs
.bluetoothctl_history
.migration_done
discogsCallback.json

# Editor config - personal, and launch.json hardcodes this machine's paths
.vscode/
