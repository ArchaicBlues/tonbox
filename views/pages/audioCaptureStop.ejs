<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">

    <link rel="icon" type="image/png" sizes="192x192" href="/images/favicon/android-chrome-192x192.png">

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

    <!-- Fortschrittsanzeige des Analog/Digital Prozesses benutzt SSE -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/event-source-polyfill/0.0.9/eventsource.min.js"></script>
    <script>
        // //For starting a new connection. The client creates a new instance of  EventSource. 
        // //It will send a new HTTP GET request to the server...

let es;
const lm = document.getElementById("myDIV")
    lm.classList.add("show");
    lm.style.display = "flex";
    es = new EventSource("/funcSSE");
    es.addEventListener("message", function(event) {
        console.log("event.data="+event.data);
        lm.innerText = event.data;
    });
    es.addEventListener("result", function() {
        es.close();
    });
//}






        // let show = ""
        // const es = new EventSource("/funcSSE");

        // const listener = function (event) {
        //     //See window console "Network" and than "Logs" in Firefox Developer tools
        //     console.log("Event from " + es.url + " Text: " + event.data + ", Type:" + event.type);

        //     // Display the event data in the `content` div
        //     var x = document.getElementById("myDIV");

        //     //window.console.log(`${type}: ${event.data || es.url}`);
        //     if (event.type === 'result') {
        //         console.log(event.data)
        //         if (event.data.match("sseEndDisc")){
        //             es.close();
        //             ip=event.data.split(" ")
        //             window.location.href="http://"+ip[1]+":8000/sseEndDisc"
        //             return
        //         }
        //         if (event.data.match("sseEndTape")){
        //             es.close();
        //             ip=event.data.split(" ")
        //             window.location.href="http://"+ip[1]+":8000/sseEndTape"
        //             return
        //         }
        //         es.close();
        //         x.innerHTML = event.data;
        //         console.log("SSE closed")
        //         return
        //     }
        //     x.innerHTML = event.data;
        //     if (event.type === 'error') {
        //         console.log("SSE error")
        //         return
        //     }
        //     if (event.type === 'open') {
        //         console.log("SSE open")
        //     }
        // };

        // //https://developer.mozilla.org/en-US/docs/Web/API/EventSource
        // es.addEventListener('open', listener);//Fired when a connection to an event source has opened.
        // es.addEventListener('message', listener);//Fired when data is received from an event source
        // es.addEventListener('error', listener);//Fired when a connection to an event source failed to open.
        // es.addEventListener('result', listener);//Fired if connection should be close
    </script>
</head>

<body>
<main>
    <%- include('../partials/header'); %>
    <form action='../audioCaptureStop' method='POST'>
        <button class="rect-btn" type="submit" name="Back" value="<%- page %>">Back / Abbruch</button>&nbsp;&nbsp;
        <% if (!getPara) { %>
            <button class="rect-btn" type="submit" name="RecordingStop" value="<%- page %>">Stop Aufnahme und starte Analyse</button>
        <% } %>
        <br><br>
        
        <div style="font-size:10px">
            <div style="max-height: 200px; overflow-y: auto; width: 90%; margin: 0 auto; text-align: center;"> 
                <b>Prozess ...</b><br>
            </div>
            <% if (!getPara && stoptime != "0") { %>
                <b><%- stoptime %> </b>
            <% } %>
            <br>
            <div id="myDIV"></div>     
        </div>
    </form>
</main>
    <%- include('../partials/footer'); %>
<script>
document.addEventListener('click', function (event) {
  const menuToggle = document.getElementById('menu-toggle');
  const mainMenu = document.getElementById('main-menu-bar');

  if (!menuToggle || !mainMenu) return; // ← prevent crash
// Nur ausführen, wenn das Menü gerade offen ist
  if (menuToggle.checked) {
    // Prüfen, ob der Klick AUSSERHALB der Navbar (main-menu-bar) war
    // .contains prüft, ob das geklickte Element (event.target) Teil der Navbar ist
    if (!mainMenu.contains(event.target)) {
      menuToggle.checked = false;
    }
  }
});    
</script>

</body>
</html>
